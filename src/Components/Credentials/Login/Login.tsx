import React from 'react';

import Credentials from '../Credentials';

const Login : React.FC = () => {
    return (
        <Credentials>
            Login
        </Credentials>
    )
}

export default Login;