import React from 'react';

import FooterStyles from './Footer.module.scss';

const Footer : React.FC = () => (
    <footer className={FooterStyles.Footer}>
        Planterium service contains all rights to these plants.
    </footer>
)

export default Footer;